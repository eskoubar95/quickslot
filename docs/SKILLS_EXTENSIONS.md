# Supplementary skills (QuickSlot extension)

Searched / referenced via skills.sh ecosystem:

- **Prisma / Postgres:** `npx skills find prisma` (use official Prisma docs + `@prisma/client` in repo)
- **shadcn/ui:** `npx skills find shadcn` — components installed via `npx shadcn@latest add …`
- **Framer Motion:** `npm install framer-motion` + React 19 compatible usage in client components

Existing global skill retained: **vercel-react-best-practices** (React/Next patterns).
